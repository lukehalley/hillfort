<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.wit.hillfort">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:name="org.wit.hillfort.main.MainApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <uses-library android:name="org.apache.http.legacy" android:required="false" />

        <activity android:name="org.wit.hillfort.activities.HillfortSplashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyA7rn6Aaj8O8IZhIM3LTrmT-O3-xqjcd9A" />

        <activity
            android:name="org.wit.hillfort.activities.MapsActivity"
            android:label="@string/title_activityMaps" />

        <activity
            android:name="org.wit.hillfort.activities.HillfortListActivity"
            android:label="@string/title_HillfortList" />

        <activity
            android:name="org.wit.hillfort.activities.HillfortActivity"
            android:parentActivityName="org.wit.hillfort.activities.HillfortListActivity"
            android:label="@string/title_HillfortAdd" />

        <activity
            android:name="org.wit.hillfort.activities.HillfortLoginActivity"
            android:label="@string/title_HillfortLogin" />

        <activity
            android:name="org.wit.hillfort.activities.HillfortRegisterActivity"
            android:parentActivityName="org.wit.hillfort.activities.HillfortLoginActivity"
            android:label="@string/title_Register" />

    </application>

</manifest>